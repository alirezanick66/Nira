## ۰. پیش‌فرض فنی

- استفاده اجباری از Python 3.11+
- استفاده از قابلیت‌های مدرن مانند:
    - `Self` برای type hint
    - `TaskGroup` برای concurrency

---

## ۱. ساختار ماژولار

- تفکیک دقیق لایه‌ها:
    - Entry Points (API / CLI)
    - Business Logic (Services)
    - Data (Repositories / Models)
    - Config

- هر فایل فقط یک مسئولیت داشته باشد (SRP)

---

## ۲. استاندارد نام‌گذاری

- فایل و تابع: `snake_case`
- کلاس: `PascalCase`
- ثابت: `UPPER_CASE`
- تمام نام‌ها:
    - انگلیسی
    - گویا
    - بدون ابهام

---

## ۳. مستندسازی و RTL

- برای تمام توابع و کلاس‌ها docstring فارسی بنویس
- کامنت inline فقط برای منطق پیچیده مجاز است
- در محیط‌های متنی، از کاراکترهای کنترل جهت (مثل `U+200F`) برای نمایش صحیح متون فارسی استفاده کن

---

## ۴. پروتکل تصمیم‌گیری (Critical)

قبل از هرگونه تولید کد، این مراحل باید اجرا شوند:

1. تحلیل کامل درخواست کاربر
2. بررسی موارد زیر:
    - آیا اطلاعات کافی است؟
    - آیا درخواست مبهم است؟
    - آیا نیاز به فایل یا context پروژه وجود دارد؟

3. اگر هرکدام از شرایط زیر برقرار بود:
    - اطلاعات ناقص است
    - فایل‌های مورد نیاز ارائه نشده‌اند
    - ساختار پروژه نامشخص است
    - درخواست مبهم است

    ⛔️ باید:
    - سوال شفاف بپرسی
    - اطلاعات یا فایل درخواست کنی
    - کاملاً متوقف شوی
    - به هیچ عنوان کد تولید نکنی

---

## ۵. منبع حقیقت (Source of Truth)

- ساختار پروژه فقط باید از `ARCHITECTURE.md` استخراج شود
- اگر این فایل:
    - موجود نبود
    - ناقص بود
    - یا در اختیار قرار نگرفته بود

⛔️ باید:

- از کاربر درخواست شود
- هیچ فرضی درباره ساختار پروژه زده نشود

---

## ۶. عدم حدس‌زدن (Anti-Hallucination)

- هرگز:
    - ساختار فایل‌ها را حدس نزن
    - محتوای فایل‌ها را فرض نکن
    - API یا مدل داده‌ای خیالی نساز

- در صورت عدم اطمینان:
    - سوال بپرس
    - صبر کن

---

## ۷. چرخه تأیید و تولید کد

فقط در صورت کامل بودن اطلاعات:

### گام ۱:

- توضیح کوتاه:
    - رویکرد
    - معماری انتخابی
    - مزایا و معایب

### گام ۲:

- توقف کامل
- انتظار برای تأیید صریح کاربر

### گام ۳ (تولید کد):

- تغییرات ساختاری:
    - ارسال کل فایل‌های تغییرکرده، هرکدام به صورت جداگانه

- تغییرات جزئی:
    - فقط بخش تغییر یافته
    - همراه با:
        - نام کلاس یا تابع والد
        - استفاده از `# ...` برای بخش‌های بدون تغییر

---

## ۸. مدیریت خطا و لاگ

- استفاده از `try/except` فقط برای خطاهای قابل پیش‌بینی
- استفاده از `Exception` کلی فقط در Entry Point
- استفاده اجباری از `logging`
- استفاده از `print` ممنوع

---

## ۹. تایپ و اعتبارسنجی

- استفاده از type hint برای:
    - تمام پارامترها
    - تمام خروجی‌ها

- استفاده از `Pydantic` برای مدل داده
- استفاده از `Any` ممنوع

---

## ۱۰. امنیت و تنظیمات

- عدم استفاده از:
    - password
    - token
    - secret

در کد (Hardcode)

- استفاده از:
    - `.env`
    - config مرکزی

---

## ۱۱. کیفیت کد

- رعایت:
    - KISS
    - SOLID

- اجتناب از:
    - over-engineering
    - anti-pattern
    - کد deprecated

---

## ۱۲. تست‌پذیری

- طراحی به صورت testable
- استفاده از:
    - Dependency Injection
- عدم وابستگی مستقیم به:
    - دیتابیس
    - API خارجی

---

## ۱۳. مدیریت وابستگی به فایل‌ها

اگر تولید کد یا تصمیم‌گیری وابسته به فایل‌ها باشد:

⛔️ باید:

- فایل‌ها درخواست شوند
- تولید کد متوقف شود

✅ هرگز:

- از روی حدس کد تولید نکن

---

## 14. تغییرات معماری

در صورت تغییر:

- design pattern
- library اصلی
- ساختار پروژه

باید در `ARCHITECTURE.md` ثبت شود:

- دلیل فنی (Rationale)
- اثرات جانبی (Side Effects)

---

## 15. رفتار در برابر درخواست‌های مشکل‌دار

اگر درخواست کاربر:

- غیرمنطقی بود
- ناقص بود
- یا باعث تولید کد بی‌کیفیت می‌شد

باید:

- صریح توضیح داده شود
- پیشنهاد بهتر ارائه شود
- از اجرای مستقیم درخواست خودداری شود

---

## 16. سبک پاسخ

- پاسخ‌ها:
    - شفاف
    - دقیق
    - بدون حاشیه

- از:
    - توضیح اضافه
    - تکرار
    - ابهام

پرهیز شود

---

## 17. مدیریت نسخه‌ها و Breaking Changes

- هرگونه تغییر در:
    - API
    - Schema داده
    - Contract بین سرویس‌ها

باید با Semantic Versioning مدیریت شود

- در صورت Breaking Change:
    1. ابتدا Deprecation Warning اضافه شود
    2. در مستندات ثبت شود
    3. در نسخه بعدی حذف گردد

- Agent باید:
    - تغییرات ناسازگار را صریح اعلام کند
    - از اعمال Silent Breaking Change خودداری کند

---

## 18. معیارهای پذیرش کد

قبل از ارسال کد، Agent باید این موارد را بررسی کند:

- آیا کد testable است؟
- آیا لاگ‌گذاری مناسب دارد؟
- آیا خطاهای محتمل مدیریت شده‌اند؟
- آیا با ARCHITECTURE.md سازگار است؟
- آیا از اصول SOLID و KISS پیروی می‌کند؟

⛔️ در صورت عدم رعایت هرکدام:

- باید قبل از ارسال، اصلاح انجام شود
- یا مشکل به کاربر گزارش داده شود

---

## 19. اولویت‌بندی قوانین

در صورت بروز تضاد بین دستورالعمل‌ها، اولویت به ترتیب زیر است:

1. پروتکل توقف و تصمیم‌گیری (بخش ۴)
2. عدم حدس‌زدن (Anti-Hallucination) (بخش ۶)
3. منبع حقیقت (Source of Truth) (بخش ۵)
4. سایر قوانین

---
