۰. **پیش‌فرض فنی:** استفاده اجباری از Python 3.11+ و ویژگی‌های مدرن (مانند `Self` برای تایپ‌دهی متدها و `TaskGroup` برای مدیریت همروندی).

۱. **ساختار ماژولار:** تفکیک دقیق لایه‌ها: Entry Points (API/CLI)، Business Logic (Services)، Data (Repositories/Models) و Config. هر فایل باید دقیقاً یک مسئولیت داشته باشد (SRP).

۲. **استاندارد نام‌گذاری:** - فایل و تابع: `snake_case`

- کلاس: `PascalCase`
- ثابت: `UPPER_CASE`

- تمام نام‌ها باید انگلیسی، گویا و با مسما باشند.

۳. **مستندسازی و RTL:** برای تمام توابع/کلاس‌ها docstring فارسی بنویس. کامنت درون‌خطی فقط برای منطق‌های بسیار پیچیده مجاز است. در محیط‌های متنی، از کاراکترهای کنترل جهت (مثل `U+200F`) برای نمایش صحیح متون فارسی استفاده کن.

۴. **چرخه تأیید و خروجی کد:**

- **گام ۱:** توضیح کوتاه رویکرد، معماری انتخابی و بررسی مزایا/معایب گزینه‌ها.
- **گام ۲:** توقف کامل و انتظار برای تأیید صریح کاربر.
- **گام ۳ (ارسال کد):** برای تغییرات ساختاری، فایل کامل را بفرست. برای تغییرات جزئی، فقط بلاکِ تغییر یافته را با ذکر تابع/کلاس والد و استفاده از `# ...` برای بخش‌های بدون تغییر ارسال کن.

۵. **مدیریت خطا و لاگ:** استفاده از `try/except` فقط برای خطاهای احتمالی پیش‌بینی‌شده. مدیریت `Exception` کلی فقط در بالاترین لایه (Entry Point). استفاده اجباری از ماژول `logging` به جای `print`. 🛠️

۶. **تایپ و اعتبارسنجی:** استفاده از Type hint برای تمام پارامترها و خروجی‌ها اجباری است. برای مدل‌سازی داده‌ها از `Pydantic` استفاده کن. استفاده از `Any` تحت هر شرایطی ممنوع است.

۷. **امنیت و تنظیمات:** هیچ‌گونه اطلاعات حساسی (Password/Token) نباید در کد Hardcode شود. استفاده از `.env` یا فایل‌های `Config` مرکزی اجباری است.

۸. **کیفیت کد (KISS & SOLID):** کد باید ساده، مدرن و عاری از Anti-patterns (مانند کدهای منسوخ) باشد. از پیچیدگی غیرضروری (Over-engineering) پرهیز کن.

۹. **عدم حدس‌زدن:** اگر در مورد نیازمندی یا بخشی از کد ابهام داری، هرگز حدس نزن. سوال بپرس و تا دریافت پاسخ شفاف منتظر بمان.

۱۰. **تست‌پذیری و وابستگی‌ها:** کد باید به‌گونه‌ای طراحی شود که قابل تست باشد (Testable Design). وابستگی‌ها را جداسازی کن (Dependency Injection) تا لایه‌ی منطق تجاری به Side Effectها (مثل دیتابیس یا API خارجی) وابسته نباشد.

۱۱. **فایل راهنما:** - برای تغییرات کوچک: فقط توضیح متنی کوتاه.

- برای تغییرات بزرگ: ساخت فایل `CHANGES.md` الزامی است. در این صورت، توضیحات متنی در چت باید بسیار کوتاه باشد.

۱۲. **مستندسازی تغییرات معماری:** هرگاه تصمیمی برای تغییر الگوی طراحی، کتابخانه اصلی یا ساختار لایه‌ها گرفته می‌شود، باید دلیل فنی (Rationale) و اثرات جانبی (Side Effects) آن در فایل در `ARCHITECTURE.md` ثبت شود.
