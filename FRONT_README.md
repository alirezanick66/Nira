# 🖥️ مستندات فرانت‌اند و راهنمای اتصال API (Demo & Integration)

## 🔌 راهنمای اتصال API (مخصوص فروشگاه‌ها)

این بخش، قرارداد رسمی اتصال سرویس Nira به پنل یا اپلیکیشن فروشگاه‌ها را تعریف می‌کند.

1. **REST JSON (`POST`):** قرارداد رسمی و پایدار برای فروشگاه‌ها (کش‌پذیر و ساختاریافته).
2. **SSE Streaming (`GET`):** پروتکل اختیاری برای نمایش وضعیت لحظه‌ای (مناسب UX دمو و ویجت‌های چت).

---

### 🔑 احراز هویت (Authentication)

تمام درخواست‌ها باید با کلید اختصاصی فروشگاه احراز هویت شوند. روش ارسال بسته به پروتکل متفاوت است:

- **روش استاندارد (REST/B2B):** ارسال هدر `X-API-Key` در درخواست:

```http
X-API-Key: <YOUR_STORE_API_KEY>
```

⚠️ در صورت عدم ارسال کلید، پاسخ `401 Unauthorized` دریافت خواهید کرد. کلید اختصاصی پس از ثبت‌نام در پنل مدیریت Nira صادر می‌شود.

- **روش دمو/ویجت (SSE):** ارسال کلید به صورت پارامتر در URL (به دلیل محدودیت‌های فنی `EventSource` مرورگر):

```http
GET /api/v1/search/stream?api_key=<YOUR_KEY>&query=...
```

⚠️ **نکته امنیتی:** کلید API هرگز نباید در سمت کلاینت (Client-Side) هاردکد شود. برای محیط پروداکشن، درخواست‌ها باید از سرور شما (Backend-for-Frontend) به نیرا ارسال شوند.

---

### 📥 فرمت درخواست (`POST /api/v1/search`)

این Endpoint برای یکپارچه‌سازی با فروشگاه‌ها طراحی شده است و خروجی ساختاریافته JSON برمی‌گرداند.

```json
{
  "query": "گوشی گیمینگ زیر ۳۰ میلیون",
  "client_session_id": "store_user_98765",
  "domain": "mobile",
  "top_k": 2
}
```

|       فیلد        |  نوع   | الزامی |                                            توضیحات                                            |
| :---------------: | :----: | :----: | :-------------------------------------------------------------------------------------------: |
|       query       | string | ✅ بله  |                                      متن محاوره‌ای کاربر                                      |
| client_session_id | string | ❌ خیر  | شناسه نشست از سمت کلاینت. برای پشتیبانی از `refine` (اصلاح نتایج قبلی) ارسال آن توصیه می‌شود. |
|      domain       | string | ❌ خیر  |         پیش‌فرض: `mobile`. در فازهای بعدی برای سوئیچ بین هدفون/لپ‌تاپ استفاده می‌شود.         |
|       top_k       |  int   | ❌ خیر  |                             تعداد محصولات پیشنهادی. پیش‌فرض: `2`                              |

---

### 📤 فرمت پاسخ استاندارد

```json
{
  "status": "success",
  "intent": "search",
  "results": [
    {
      "product_id": 17013890,
      "title": "گوشی موبایل شیائومی مدل Poco M5s ...",
      "price": 38500000,
      "price_range": "premium",
      "camera_quality": "good",
      "tags": ["gaming", "battery_heavy"]
    }
  ],
  "applied_filters": { "price": {"<=": 30000000}, "ram_gb": {">=": 8} },
  "warnings": [],
  "session_id": "nira_sess_abc123",
  "explanation": "این دو مدل نزدیک‌ترین گزینه‌ها به بودجه و نیاز گیمینگ شما هستند...",
  "next_suggestion": "می‌توانید بودجه را تا ۳۵ میلیون افزایش دهید یا برند خاصی را فیلتر کنید."
}
```

### ⚙️ محدودیت‌ها و Best Practices

- `Rate Limit`: ۶۰ درخواست در دقیقه (بر حسب پلن اشتراک قابل ارتقا)
- `Timeout`: حداکثر ۵ ثانیه برای پاسخ کامل
- در کوئری‌های `refine`، حتماً `client_session_id` مرحلهٔ قبل را ارسال کنید.
- فیلد `warnings` را در فرانت نمایش دهید تا کاربر از حذف خودکار فیلترهای سخت‌گیر آگاه شود.

---

## ⚡ ویژگی‌های پیاده‌سازی‌شده (دموی فعلی)

|             ویژگی             |                                                                                    توضیح فنی                                                                                     |
| :---------------------------: | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------: |
|  💾 **حافظهٔ چت و Session**   |                                             تولید خودکار `session_id` و ذخیره در `localStorage` برای حفظ Context مکالمه بین رفرش‌ها                                              |
|  ⚡ **دکمه‌های تعاملی سریع**   | تولید پویای کوئری‌ها بر اساس نتایج قبلی:<br>• `ارزان‌تر` → محاسبه سقف جدید (۸۰٪ کمترین قیمت)<br>• `گران‌تر` → محاسبه کف جدید (۱۳۰٪ بیشترین قیمت)<br>• `گزینهٔ بعدی` → کوئری ثابت |
| 🛍️ **کارت محصول (حداکثر ۲)** |                                                   نمایش `image_url`، `title`، `price`، `price_range`، `camera_quality`، `tags`                                                   |
|   🎨 **شخصی‌سازی تم زنده**    |                                          متغیرهای CSS (`:root`) + پنل تنظیمات → تغییر رنگ اصلی، حالت روشن/تاریک، ذخیره در LocalStorage                                           |
|   📡 **بازخورد وضعیت شفاف**   |                                                        مراحل: `⏳ تحلیل NLU` → `🔍 جستجو` → `⚖️ مرتب‌سازی` → `💬 پاسخ LLM`                                                        |
|     **📊 پنل آنالیتیکس B2B**      |                                                    داشبورد ایزوله Vanilla + ES Modules، بدون SPA/Router، کاملاً جدا از چت دمو                                                    |
|      **📈 چارت‌های تعاملی**       |                                               نمودار میله‌ای ساعتی + دونات نیت‌ها با Chart.js (CDN)، رندر پویا بر اساس بازه زمانی                                                |
|     **🗓️ فیلتر زمانی پویا**      |                                                     تجمیع آمار بر اساس `days` (1/7/30 روز) سمت دیتابیس، تغییر بدون رفرش صفحه                                                     |
|      **🔄 بارگذاری خودکار**       |                                                     Toggle + شمارش معکوس 60 ثانیه‌ای + Skeleton Loading + Stagger Animation                                                      |
|     **🔍 ردیابی مدل و توکن**      |                                                         نمایش `model_used` (Groq/Gemini/Fast-Path) + مصرف توکن در هر لاگ                                                         |

## 🛠️ معماری فنی (Current Demo)

- **تکنولوژی:** `HTML5 + CSS3 + Vanilla JavaScript (ES Modules)`
- **سروینگ:** مستقیم توسط FastAPI روی مسیر `/`
- **ارتباط با بک‌اند:**
    - دمو: `EventSource` به `/api/v1/search/stream` (برای نمایش وضعیت زنده)
    - فروشگاه‌ها: `Fetch API` به `/api/v1/search`

---

## 📂 ساختار فایل‌ها

```
frontend/
├── index.html            # ساختار معنایی، لینک به استایل‌ها و اسکریپت‌ها
├── css/
│   ├── 01-base.css       # توکن‌های طراحی، ریست و تم
│   ├── 02-layout.css     # چیدمان صفحه و حالت‌های Welcome/Chat
│   ├── 03-components.css # استایل حباب‌ها، کارت‌ها و اکشن‌ها
│   ├── 04-admin.css              # توکن‌ها و استایل‌های اختصاصی پنل B2B
│   └── style.css
└── js/
    ├── admin/                     # ماژول‌های ایزوله پنل ادمین
    │   ├── admin-api.js     # لایه ارتباط با `/api/v1/admin/*`
    │   ├── admin-ui.js        # رندر DOM، چارت‌ها، جدول، skeleton
    │   └── admin.js              # نقطه ورود و مدیریت state
    ├── script.js                   # نقطه ورود (Orchestration) و مدیریت رویدادها
    ├── session.js                # مدیریت session_id و localStorage
    ├── ui.js                           # توابع رندر DOM (پیام‌ها، کارت‌ها، وضعیت)
    └── api.js                        # لایه ارتباط با SSE و مدیریت Callbackها

```

---

## 🚀 راه‌اندازی و اجرا

```bash
# ‫1. تنظیم محیط مجازی و نصب وابستگی‌ها
uv sync

# ‫2. کپی و تنظیم متغیرهای محیطی (ضروری)
cp .env.example .env
# ⚠️ فایل .env را باز کنید و GROQ_API_KEY, QDRANT_URL, QDRANT_API_KEY را تنظیم نمایید.

# ‫3. اعمال تغییرات دیتابیس
uv run alembic upgrade head

# ‫4. اجرای سرور
uv run uvicorn src.api.main:app --reload --host 0.0.0.0 --port 8000

# ‫5. دسترسی به رابط کاربری
http://localhost:8000/
```

---

## 🗺️ چشم‌انداز Post-MVP

|        دسته         |                             نمونه فیچرها                             |
| :-----------------: | :------------------------------------------------------------------: |
| 🛒 **یکپارچه‌سازی** |       اتصال به سبد خرید فروشگاه، افزودن با یک کلیک، OAuth 2.0        |
|  📊 **آنالیتیکس**   | داشبورد «شکاف موجودی»، «نیازهای پرجستجو»، محصولات پربازدید بدون خرید |
| 🎨 **تجربه کاربری** |     استریم واقعی SSE، پشتیبانی از ورودی صوتی، حالت آفلاین (PWA)      |
|  📱 **ریسپانسیو**   |           بهینه‌سازی برای موبایل، منوی همبرگری، لمسی بهینه           |

---

**نسخه:** 2.2.0 | **آخرین به‌روزرسانی:** 2026/05/30


